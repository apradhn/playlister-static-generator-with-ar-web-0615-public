<!doctype html>
<html>
    <head>
        <title>Genres</title>
    </head>
    <body>
        <h1>Genres</h1>
        <ul>
          <% @genres.each do |genre| %>
            <li><a href="<%= genre.to_slug %>.html"><%= genre.name %></a></li>
           <% end %>  
        </ul>
    </body>
</html>